import styled from "./styled";

export const footer = styled.css `

    & [data-popup-window] > [data-window-content] > [data-layout=command-row] {
        margin: -7px;
        margin-top: 5px;
        border-radius: 0;
    }


`.expand("body");